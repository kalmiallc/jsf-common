export interface TargetLanguage {
  /**
   * Language identifier. Recommended to use the 2-character ISO 639-1 code.
   */
  code: string;
  /**
   * Friendly name to display in the interface.
   */
  name: string;
}
